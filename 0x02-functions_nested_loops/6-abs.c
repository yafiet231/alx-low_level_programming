#include "main.h"

/**
 * -abs - computes the absolutevalue of an integer
 * @n: the int to check
 * return: the absolute value of int
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
